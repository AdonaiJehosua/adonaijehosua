const { createTheme } = require("@mui/material");

const mainTheme = createTheme({
    components: {
        MuiModal: {
            styleOverrides: {
                
            }
        }
    }
})